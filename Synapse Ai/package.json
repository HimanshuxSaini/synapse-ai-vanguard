{
  "name": "synapse-ai-vanguard",
  "version": "1.0.0",
  "private": true,
  "description": "Synapse AI Vanguard — Premium Neural Learning Platform",
  "scripts": {
    "build": "cd client && npm run build",
    "netlify-build": "cd client && npm install && npm run build"
  }
}
